export default {
  n: 't68', name: 'Falloff',
  idea: 'Contour falloff run around the whole silhouette, tail included, so the bubble reads as lit volume rather than a flat panel.',
  ground: 'light',
  palette: ['#200c47', '#813fd9', '#e6b6fb', '#ffffff'],
};
