export default {
  n: 'm07',
  name: 'Window',
  app: 'mozz',
  object: 'Cassette shell, two windows',
  move: 'occlusion',
  reading: 'eyes',
  sentence: 'The ZZ is the slack of the tape framed by the two hub windows.',
  idea:
    'A closed cassette shows tape only where a window lets it through, so the ribbon folds back on itself twice and the shell hides the run between — that is what makes two separate Zs instead of one long zigzag. Every bar is cut off by the window rim, so the letters cannot exist without the object, and the head opening in the bottom edge is the mouth.',
  palette: ['#e0243f', '#6d0f1e', '#f58a99'],
};
