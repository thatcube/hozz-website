export default {
  title: 'Empty on purpose',
  says: 'Hozz has no default destination, so the middle of the picture is a slot left open for a decision only you can make.',
  note: 'The centre is a crisp dashed slot, centred and labelled, with a constant gap held all the way round it so the void reads as reserved rather than missing. Dashed leader lines run inward from each type and stop short. The chips step 45° at a time and then rest, parked on a route that was drawn but never connected.',
  by: 'orb05 · Claude Opus 5',
};
