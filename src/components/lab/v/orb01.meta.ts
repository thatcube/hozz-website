export default {
  title: 'One track',
  says: 'Everything Apple holds runs on one track, and what it leaves you is a file.',
  note: 'The centre is the artefact: a sheet with a dog-ear named export.ndjson, at rest. The path is a real hairline circle ticked like a dial, static, so the chips read as records on a track. One ring, eight types evenly spaced, clockwise at a single speed — two minutes a turn — each pill straddling the line so the track threads through it.',
  by: 'orb01 · Claude Opus 5',
};
