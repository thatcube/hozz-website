export default {
  n: '28',
  name: 'Draft',
  idea: 'One ribbon that narrows as it travels — wide where it enters the first Z, narrow where it leaves the second — so the mark has a direction.',
  face: 'none',
  note: 'Two tones: face and folded diagonal. The taper is continuous across both letters, so the pair reads as one run of ribbon.',
};
